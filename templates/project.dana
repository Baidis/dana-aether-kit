# project.dana — entry point for {{project_name}}
# Run with: aether run  (defaults to this file)

from agents.{{agent_name}} import run_{{agent_name}}

if __name__ == "__main__":
    result = run_{{agent_name}}("your input here")
    print(result)
