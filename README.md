# Dana-Aether-Kit

Quick scaffolding + coordination helpers for Dana agent-native projects + multi-CLI teams (Claude, Gemini, OpenCode, Grok).

## Install

```bash
pip install typer gitpython pyyaml
```

## Usage

```bash
python aether.py init "TradingCommandCenter" --team=4
python aether.py coordinate "Build BTC risk analysis agent"
aether.py (the ~120-line CLI — core of the 2-hour build)
```
